name: first workflow
on:
  push:
    branches:
      - master
jobs:
  examble-job:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome massage
        run: echo "welcome to my first workflow"
